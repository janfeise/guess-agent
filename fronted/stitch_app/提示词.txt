你是一名前端架构师，精通 uni-app、Vue3以及移动端交互设计

当前我有一份由 Google Stitch 生成的 App 原型 HTML 文件（仅包含静态结构和样式，没有任何交互逻辑）

你的任务是：基于该 HTML 原型，重构为一个完整的 uni-app 项目，并补全合理的交互逻辑

一、理解与分析
解析 HTML 结构，识别页面模块（如：首页、列表页、详情页、个人中心等）
提取可复用组件（如：导航栏、卡片、列表项、按钮等）
判断页面层级关系与跳转逻辑
二、项目生成要求

请基于 uni-app 规范，生成完整项目结构，包括：

pages 目录（按页面拆分）
components 目录（抽离公共组件）
static 资源目录
pages.json 配置（页面注册、tabBar 等）
三、页面实现要求

对每一个页面：

将 HTML 转换为 uni-app（Vue）写法
使用 <template> + <script> + <style> 结构
样式尽量还原（可适当优化为 rpx）
不要直接复制原 HTML，而是结构化重写
四、交互补全（重点）

原 HTML 无交互，你需要根据 UI 推断并补全：

页面跳转（使用 uni.navigateTo / switchTab）
列表点击进入详情
按钮点击反馈（loading / toast）
表单输入与提交逻辑（如有）
Tab 切换 / 筛选 / 展开收起

要求：

交互必须符合真实 App 使用习惯
不允许空交互（所有可点击元素必须有行为）
五、代码规范要求
使用 Vue 组合式 API（如 setup，若适合）
逻辑清晰，避免冗余代码
组件拆分合理（避免页面过大）
使用 uni.showToast 等实现基础反馈
六、输出格式

请按以下结构输出：

项目结构树
pages.json
关键页面代码（完整示例）
公共组件代码
交互说明（简要说明每个页面的行为）

所有代码使用代码块包裹，确保可直接复制使用

强制要求：

不允许只做 HTML → Vue 的简单转换
必须补全完整交互逻辑，否则视为失败
每个可点击元素必须绑定事件
至少实现 3 类交互：跳转 / 状态变化 / 用户反馈
必须抽离公共组件（不少于 2 个）

如果某些交互无法从 UI 判断，请基于常见 App 设计模式进行合理推断，并说明你的设计依据

在生成代码前，请先输出：

页面拆分方案
组件拆分方案
交互设计说明

确认后再生成代码